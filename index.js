/**
 * @format
 */

import { AppRegistry } from "react-native";
import BOTViewer from "./BOTViewer";

import { name as appName } from "./app.json";

AppRegistry.registerComponent(appName, () => BOTViewer);
